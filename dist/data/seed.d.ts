/**
 * Seed data
 *
 * Two customers and two documents in the shape the backend returns, used to
 * populate the in-memory store on first load.
 */
import type { Customer, Document } from "./types.js";
export declare const seedCustomers: Customer[];
export declare const seedDocuments: Document[];
